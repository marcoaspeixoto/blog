<!DOCTYPE html>
<html>
  <head>
    <title>Blog do Marco Peixoto</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <div class="bg-primary text-white text-center mb-4 p-4">
      <%= link_to root_path, class:'text-white text-decoration-none' do %>
        <h1 class="display-5">Blog do Marco Peixoto</h1>
      <% end %>
      <p class="lead">
        Olá, aqui você encontrará artigos sobre programação, tecnologia e carreira.
      </p>
    </div>

    <div class="container">
      <% flash.each do |type, message| %>
      <div class="alert alert-<%= type == 'alert' ? 'darger' : 'sucess' %> %> alert-dismissible fade show"
           role="alert">
        <%= message %>

        <button type="button" class="close" data-dismiss="alert" aria_label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <% end %>

      <%= yield %>
    </div>

    <footer class="bg-primary text-white text-center mt-5 p-4">
      Copyright © Marco Peixoto <%= Date.current.year %>
    </footer>

  </body>
</html>
